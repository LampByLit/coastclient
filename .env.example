# Production: Railway service origin ONLY (no path). We append /api automatically — same as interior /api/*.
# Example: VITE_QUOTE_API_BASE=https://your-app.up.railway.app
# Leave unset for local dev (Vite proxies /api → localhost:3001).
# Railway API service: set CORS_ORIGINS to this website's origin(s).
